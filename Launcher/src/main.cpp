#include "pch.h"
#include "Engine/test.h"

/*
DO

Vscode setup

Typedefs

Common macros

Module system; Use launcher to input modules to dynamically link to

Module callback function system

Log system (spdlog)

Basic memory allocation (Global, prealloc budget, fallback)

*/

int main(/*char** argv, int argc*/) {
	do_the_test();

	return 0;
}